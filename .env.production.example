# Copy to .env.production.local (gitignored) for syncing cards to Vercel.
# Vercel Dashboard → Project → Settings → Environment Variables → DATABASE_URL (Production)

PRODUCTION_DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
